# 변경 기록

## v1.0.7-rc4 — 토픽 연결 수동 검수 완료 후보판 — 2026-08-07

- INFO를 `(부모 DIAL, INFO INAM)` 문맥으로 식별하여 기존 INFO ID 충돌 누락 66개 복구를 유지했습니다.
- 원본 `Morrowind.esm`, `Tribunal.esm`, `Bloodmoon.esm`과 다시 대조하여 Topic/Persuasion 누락 INFO 153개를 추가 복구하고 한국어화했습니다.
- compiled MWScript의 영문 `AddTopic` 참조 16개를 직접 수정했습니다.
- 흐리스카르 `금화 되찾기`와 프로케수스 살해 토픽 경로를 복구했습니다.
- 프로케수스 살해 INFO의 잘못 번역된 기술용 `ANAM` 중복 레코드 1쌍을 제거했습니다.
- OpenMW 0.51 `.mrk`를 이용해 안전한 implicit topic keyword만 복구했습니다.
- 자동 보류했던 토픽 36개를 전부 수동 검수하여 미결 0으로 정리했습니다.
- 전체 대사 재검증에서 새 false-positive topic edge 0, 기존 정상 edge 손실 0을 확인했습니다.
- 잘려 있던 `One destiny`, `Sixth trial`, `skin of the pearl` 응답을 원문 기준으로 복원했습니다.
- `CELL=0`, `PGRD=0`, `.top` 없음, 구형 `@ + 0x7F + #` 링크 없음 상태를 유지합니다.

## v1.0.5 — cfg 없는 가독성 폰트판 — 2026-08-05

- `openmw.cfg`를 포함하거나 덮어쓰는 배포 방식을 폐기했습니다.
- OpenMW 0.51 기본 폰트 이름을 사용하는 SmallBatang 보정 폰트를 모드에 포함했습니다.
- 받침 글자만 13px로 진행하던 메트릭을 12px로 통일했습니다.
- 받침과 윗글자가 붙는 문제를 줄이기 위해 받침을 한 표시 픽셀 아래로 이동했습니다.
- OpenMW 0.51의 초성 `ㅎ` 슬롯 문제를 보정했습니다.
- 최신 ESP의 `CELL=0`, `PGRD=0`을 검증했습니다. 셀 이름 변경으로 저장 파일 이동이 깨지는 문제가 없습니다.

## v01 핫픽스 1 간편 설치 수정 — 2026-08-03

- 사용자가 제공한 OpenMW 0.51.0 기본 `openmw.cfg` 521줄 전체를 새 배포 설정의 기반으로 사용했습니다.
- 기본 조명·날씨·물·캐릭터 생성 설정을 유지했습니다.
- 한국어 모드 경로, `encoding=win1252`, 공식 ESM 3개와 한국어 ESP의 로드 순서를 추가했습니다.
- SmallBatang4 폰트 3개 항목과 한국어 fallback 문자열 63개를 교체했습니다.
- 수정된 `openmw.cfg`는 529줄, 30,121바이트입니다.
- 간편 설치 ZIP의 내부 체크섬과 저장소 배포 해시를 갱신했습니다.

## v01 핫픽스 1 — 2026-08-03

- 이전 단일 통합 ESP를 폐기했습니다.
- 실내 지명용 `CELL` 레코드를 복사할 때 원본 ESM의 `FRMR` 참조 번호를 새 플러그인의 마스터 인덱스로 재매핑하지 않은 문제를 확인했습니다.
- 이 문제로 시작 구역의 경비병과 `chargen class` 등 캐릭터 생성 오브젝트가 새 참조로 복제될 수 있었습니다.
- 문제가 있는 실내 지명 1,328개 변경을 핫픽스에서 임시 제외했습니다.
- 검증된 게임 본문·이름·추가 표시·스크립트 ESP로 되돌렸습니다.
- 핫픽스 ESP의 `CELL` 레코드에서 `chargen` 참조가 0개임을 확인했습니다.
- OpenMW UI 456개, fallback 63개, SmallBatang4 폰트는 유지했습니다.
- 문제 있는 ESP로 진행한 저장 파일에는 중복 참조나 진행 상태가 남을 수 있으므로 캐릭터 생성 전 저장 파일 또는 새 게임을 권장하도록 안내를 수정했습니다.

## v01 단일 ESP 배포본 — 폐기됨

- 본편 번역과 실내 지명 번역을 하나의 ESP로 통합했으나, 실내 `CELL` 참조 번호 처리 오류로 게임 오브젝트가 복제되는 문제가 확인되어 폐기했습니다.

## v01 — 2026-08-03

- 최종 검수표 51,440행을 반영했습니다.
- 게임 내 번역 45,881행을 재빌드하고 검증했습니다.
- 스크립트 정규화 과정에서 선택지와 메시지 상자 문법이 깨지지 않도록 수정했습니다.
- 실내 지명 1,328개와 관련 참조를 검수했습니다.
- OpenMW 0.51 네이티브 UI 문자열 456개를 8개 l10n 영역에 추가했습니다.
- `openmw.cfg`용 번역 fallback 문자열 63개를 추가했습니다.
- YAML 1.1 불리언으로 오인되는 `No`, `Off`, `On`, `Yes` 키를 문자열 키로 복원했습니다.
- ㅎ 표시를 보정한 SmallBatang4 비트맵 폰트를 추가했습니다.
